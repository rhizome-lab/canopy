{
  "name": "@dusklight/renderer-json",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src && oxfmt --check src",
    "format": "oxfmt src",
    "test": "bun test",
    "build": "bun build ./src/index.ts --outdir ./dist --target browser"
  },
  "type": "module",
  "dependencies": {
    "@dusklight/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "oxfmt": "^0.36.0",
    "oxlint": "^1.51.0"
  }
}
